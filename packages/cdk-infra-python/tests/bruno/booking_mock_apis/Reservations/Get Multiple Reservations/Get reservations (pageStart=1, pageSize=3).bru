meta {
  name: Get reservations (pageStart=1, pageSize=3)
  type: http
  seq: 2
}

get {
  url: {{apiUrl}}/reservation?pageStart=1&pageSize=3
  body: none
  auth: awsv4
}

auth:awsv4 {
  accessKeyId: {{awsAccessKeyId}}
  secretAccessKey: {{awsSecretAccessKey}}
  sessionToken: {{awsSessionToken}}
  service: execute-api
  region: {{awsRegion}}
}

headers {
  X-Api-Key: {{apiKey}}
}

params:query {
  pageStart: 1
  pageSize: 3
}
