meta {
  name: Create Chicago Regency Reservation
  type: http
  seq: 1
}

post {
  url: {{apiUrl}}/reservation
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "BookingInfo": {
      "BookedBy": "Filter Test Chicago",
      "BookingDate": "2025-05-20"
    },
    "Hotel": {
      "Id": 50005,
      "Code": "HRCH",
      "Name": "Chicago Hotel"
    },
    "RoomStay": {
      "CheckInDate": "2025-03-15",
      "CheckOutDate": "2025-03-20",
      "GuestCount": [
        {
          "NumGuests": 2
        }
      ],
      "NumRooms": 1,
      "Products": [
        {
          "Product": {
            "RoomCode": "B1K",
            "RoomName": "Club Level King"
          },
          "Price": {
            "Amount": 350.0
          }
        }
      ]
    },
    "Guests": [
      {
        "PersonName": {
          "GivenName": "Filter",
          "Surname": "Chicago_{{testId}}"
        },
        "EmailAddress": [
          {
            "Type": "Primary",
            "Value": "filter_chicago_{{testId}}@example.com"
          }
        ],
        "ContactNumbers": [
          {
            "Number": "3125551234"
          }
        ]
      }
    ],
    "status": "Confirmed",
    "Currency": {
      "Code": "USD",
      "Name": "US Dollars",
      "Symbol": "$"
    },
    "RoomPrices": {
      "TotalPrice": {
        "Price": {
          "CurrencyCode": "USD",
          "TotalAmount": 1750.0,
          "TotalAmountIncludingTaxesFees": 1942.50
        }
      }
    }
  }
}

script:post-response {
  try {
    if (res.status === 200 && res.body.reservations && res.body.reservations[0]) {
      const confirmationNumber = res.body.reservations[0].CrsConfirmationNumber;
      bru.setEnvVar("chicagoConfirmationNumber", confirmationNumber);
      console.log(`✅ Stored Chicago confirmation number: ${confirmationNumber}`);
    }
  } catch (error) {
    console.log(`❌ Error processing response: ${error.message}`);
  }
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
  
  test("Response includes a valid confirmation number", function() {
    expect(res.body).to.have.property("reservations");
    expect(res.body.reservations).to.be.an('array').that.is.not.empty;
    expect(res.body.reservations[0]).to.have.property("CrsConfirmationNumber");
  });
}
